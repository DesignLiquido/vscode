classe Resposta {
    codigo: texto
    statusHttp: numero

    enviar(codigo) {
        isto.codigo = codigo
        retorna(isto)
    }

    status(statusHttp) {
        isto.statusHttp = statusHttp
        retorna(isto)
    }

    lmht(*visaoEValores) {
        se (visaoEValores.tamanho > 1) {
            escreva("Dois")
        } senão {
            escreva("Um")
        }
    }
}

var resp = Resposta()
// escreva(resp.enviar("Ok").status(200))
resp.lmht(1, 2)
